# MongoDB Configuration
# Get your MongoDB Atlas connection string from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# API Configuration
API_HOST=0.0.0.0
PORT=5000

# Python Version (for Render deployment)
PYTHON_VERSION=3.11.0

# Scraping Configuration
SCRAPE_INTERVAL_HOURS=24

# Deployment Instructions:
# 1. For local development: Copy this to .env and update MONGODB_URI
# 2. For Render deployment: Add these as environment variables in Render dashboard
# 3. Never commit .env file to GitHub (it's in .gitignore)
# 4. Update API URL in events_api.js after deploying to Render

